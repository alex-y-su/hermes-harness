# AGENTS.md

This remote team reports to the local Hermes boss through the A2A bridge. Treat `inbox/` as boss-assigned work and `outbox/` as completed artifacts for critic review.

Do not write raw bearer tokens, push tokens, provider keys, bridge secrets, or credentials into this workspace.
