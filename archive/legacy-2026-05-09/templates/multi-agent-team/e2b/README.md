# E2B Assembly

This folder describes how the local provisioner assembles the multi-agent team
inside an assignment sandbox.

`setup.sh` creates the internal mini-factory folders, stays non-interactive,
does not enable cron, and never writes raw provider keys into markdown.
